import React, { useState, useEffect } from "react";
import styled from "styled-components";

const Guidelines = () => {
  return (
    <React.Fragment>
      <h1>Community Guidelines</h1>
      <h3>
        &ensp;&ensp;Any content uploaded to the MySpotifyInsights web
        application is expected to follow community guidelines. Uploaded content
        that threatens violence, depicts harm to an individual or group,
        promotes any form of harassment, or incites hate is prohibited. If these
        guidelines are not followed, a MySpotifyInsights moderator has the right
        to remove the content and revoke the user's privileges to use the
        application.
      </h3>
    </React.Fragment>
  );
};

export default Guidelines;
